export default function App() {
  return (
    <main className="app">
      <h1>TaskFlow</h1>
      <p className="lead">React + Vite + TypeScript frontend. Run the API separately from the backend folder.</p>
    </main>
  );
}
